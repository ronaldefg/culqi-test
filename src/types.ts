export interface CardData {
    email: string;
    card_number: string;
    cvv: string;
    expiration_month: string;
    expiration_year: string;
}

export interface CardDataInfo {
    email: string;
    card_number: string;
    expiration_month: string;
    expiration_year: string;
}